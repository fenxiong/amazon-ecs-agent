language: go
go_import_path: github.com/aws/amazon-ecs-agent
sudo: false
go:
  - 1.12

matrix:
  include:
    - os: windows
      script:
        - go test -v -race -tags unit -count 10 -timeout 20m ./agent/dockerclient/dockerapi/
